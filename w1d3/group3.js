H// Array: Merge Sort
// visualization: https://www.hackerearth.com/practice/algorithms/sorting/merge-sort/visualize/

// Time Complexity
//  - Best case: O(n log(n))
//  - Average case: O(n log(n))
//   - Worst case: O(n log(n))

// MergeSort(arr[], l,  r)
// If r > l
//      1. Find the middle point to divide the array into two halves:
//              middle m = (l+r)/2
//      2. Call mergeSort for first half:
//              Call mergeSort(arr, l, m)
//      3. Call mergeSort for second half:
//              Call mergeSort(arr, m+1, r)
//      4. Merge the two halves sorted in step 2 and 3:
//              Call merge(arr, l, m, r)


// //steps:
//     1. create a merge function to merge two already sorted arrays into a single sorted array
//       - you do NOT need to work in place, ok to use a new array
//     2. create merge sort function to sort the provided array
//       - split the array into half and recursively merge the halves
//       - splitting of arrays stops when array can no longer be split
//       - an array of 1 item is by definition sorted, so two arrays of 1 item can therefore be merged into a sorted array



//   useful methods
//   - arr.concat(array2, array3, ..., arrayX)
//     - combines array arg(s) with the array that concat is called on
//     - returns new arr w/o mutating originals
//   - arr.slice([begin[, end]])
//     - returns a copy of the specified portion of the array
//     - end is exclusive
//       - if omitted, slices from provided 'begin' to end of array


function mergeSort(arr1, arr2);
    var Temp = []
    var spot = 0
    var spot2=
    for((ourArray.length % 2) !==0){
        babyArray1 === ourArray.splice[i]+[i+1]
        babyArray2 === ourArray.splice[i+2]+[i+3]
        babyArray3 === ourArray.splice[i+4]+[i+5]
        babyArray4 === ourArray.splice[i+6]+[i+7]


    ourArray=[212,54,79,35,20,11,25,99]
console.log mergeSort

function MergeSort([ourArray]){
    newArray=[]

      while (newArray < array1.length + array2.length)
      (i=0; i>ourArray.length;i++);
       newArray.length <ourArray.length
        if (ourArray.length % 2] !== 0){

            babyArray1 === ourArray.splice[i]
            babyArray2 === ourArray.splice[i+1]
            babyArray3 === ourArray.splice[i+2]
            babyArray4 === ourArray.splice[i+3]
            babyArray5 === ourArray.splice[i+4]
            babyArray6 === ourArray.splice[i+5
    {